# EoMT + SurgeNetDINO (DINOv2 ViT-L) for TEP Semantic Segmentation
# 논문 best 모델: DINOv2 ViT-L (Dice 0.77~0.79)
# img_size=504 (patch14 호환 해상도, 512에 가장 가까운 14의 배수)

trainer:
  max_epochs: 100
  logger:
    class_path: lightning.pytorch.loggers.CSVLogger
    init_args:
      save_dir: logs
      name: tep_dinov2_vitl_re_10
  callbacks:
    - class_path: training.plot_callback.TrainingCurvePlotCallback
      init_args:
        save_dir: logs/tep_dinov2_vitl_re_10/curves
    - class_path: lightning.pytorch.callbacks.EarlyStopping
      init_args:
        monitor: metrics/val_dice_fg
        patience: 30
        mode: max
    - class_path: lightning.pytorch.callbacks.ModelCheckpoint
      init_args:
        monitor: metrics/val_dice_fg
        mode: max
        save_top_k: 1
        filename: best

model:
  class_path: training.mask_classification_semantic.MaskClassificationSemantic
  init_args:
    num_classes: 5
    img_size: [504, 504]
    lr: 4e-5
    lr_mult: 0.1
    llrd: 0.8
    weight_decay: 0.01
    warmup_steps: [100, 200]
    attn_mask_annealing_enabled: True
    attn_mask_annealing_start_steps: [0, 500, 1000, 1500]
    attn_mask_annealing_end_steps: [500, 1000, 1500, 2000]
    ignore_idx: 255
    load_ckpt_class_head: False
    ckpt_path: ./weights/DINOv2_ViTl14_size336_SurgeNetXL_EoMT.pth
    network:
      class_path: models.eomt.EoMT
      init_args:
        num_q: 50
        encoder:
          class_path: models.vit.ViT
          init_args:
            backbone_name: vit_large_patch14_dinov2
            patch_size: 14

data:
  class_path: datasets.tep_semantic.TEPSemantic
  init_args:
    path: ./TEP_dataset/ro
    split_train: train_ori
    split_val: tune
    img_size: [504, 504]
    num_classes: 5
    batch_size: 4
    num_workers: 4
    color_jitter_enabled: True
    scale_range: [0.5, 2.0]
    # color_jitter_enabled: False
    # scale_range: [1.0, 1.0]